# nistula-technical-assessment
Guest message handler built with Python &amp; FastAPI. Receives webhook payloads from multiple channels (WhatsApp, Airbnb, etc.), normalises them into a unified schema, classifies query type, drafts replies via Claude API, and returns a confidence score with recommended action.
